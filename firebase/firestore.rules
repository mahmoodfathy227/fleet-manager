rules_version = '2';
// Push Notifications: clients must NOT access Firestore directly.
// All Firestore reads/writes happen only via Firebase Cloud Functions (Admin SDK).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
